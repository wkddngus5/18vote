class dragSort {
  constructor() {
    // console.log($('#sortable'));
    // $('#sortable').sortable();
    let list = document.querySelector('#sortable');
    Sortable.create(list);

    let foo = docu
  }
}

new dragSort();


